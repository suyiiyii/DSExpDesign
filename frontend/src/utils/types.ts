export interface City {
    name: string;
}

export interface Transport {
    type: string;
    name: string;
    start: string;
    end: string;
    price: number;
    start_time: string;
    end_time: string;
    run_id: string;
}